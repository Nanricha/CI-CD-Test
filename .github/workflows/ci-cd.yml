name: Deploy to Salesforce

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Salesforce CLI
        run: npm install sfdx-cli --global

      - name: Authenticate with Salesforce
        run: |
          sfdx auth:login -r ${{ secrets.SF_LOGIN_URL }} \
            -u ${{ secrets.SF_USERNAME }} \
            -p ${{ secrets.SF_PASSWORD }} \
            -i ${{ secrets.SF_CLIENT_ID }} \
            -s ${{ secrets.SF_CLIENT_SECRET }}

      - name: Deploy source to org
        run: sfdx force:source:deploy -p force-app --json --loglevel fatal